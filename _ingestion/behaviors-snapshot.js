// @ts-nocheck
// AUTO-GENERATED FILE — DO NOT EDIT
// This file is a snapshot of all behavior/animation scripts from src/scripts/*.ts
// Generated by _ingestion/build-behaviors-snapshot.js
// Date: 2025-11-26T15:38:15.779Z


// ==========================================
// ===== From src/scripts/global.ts =====
// ==========================================


function initGlobal() {
    // Initialize Lucide icons globally
    createIcons({ icons });

    // Add any other global initializations here
    console.log('Global logic initialized');
}


// ==========================================
// ===== From src/scripts/header.ts =====
// ==========================================


function initHeader() {
    // --- Header Scroll Effect ---
    const header = document.getElementById('main-header');
    const menuIcon = document.getElementById('menu-icon');

    // Only apply scroll effect on index page (transparent header)
    const isIndexPage = document.body.dataset.page === 'home';

    if (isIndexPage && header) {
        window.addEventListener('scroll', () => {
            if (window.scrollY > 50) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
        });
    }

    // --- Mobile Menu Toggle ---
    const mobileMenuBtn = document.getElementById('mobile-menu-btn');
    const mobileMenu = document.getElementById('mobile-menu');
    const mobileLinks = mobileMenu?.querySelectorAll('a');

    if (mobileMenuBtn && mobileMenu && menuIcon && header) {
        const toggleMenu = () => {
            const isOpen = mobileMenu.classList.contains('open');
            if (isOpen) {
                mobileMenu.classList.remove('open');
                header.classList.remove('mobile-menu-open');
                menuIcon.setAttribute('data-lucide', 'menu');
            } else {
                mobileMenu.classList.add('open');
                header.classList.add('mobile-menu-open');
                menuIcon.setAttribute('data-lucide', 'x');
            }
            createIcons({ icons });
        };

        mobileMenuBtn.addEventListener('click', toggleMenu);

        // Close mobile menu when a link is clicked
        mobileLinks?.forEach(link => {
            link.addEventListener('click', () => {
                if (mobileMenu.classList.contains('open')) {
                    toggleMenu();
                }
            });
        });
    }

    createIcons({ icons });
}


// ==========================================
// ===== From src/scripts/page-contact.ts =====
// ==========================================

function initPage() {
    console.log('Page: Contact initialized');

    const form = document.querySelector('form');
    if (form) {
        form.addEventListener('submit', (e) => {
            e.preventDefault();
            alert('Merci pour votre message ! (Simulation)');
        });
    }
}


// ==========================================
// ===== From src/scripts/page-domaine.ts =====
// ==========================================

function initPage() {
    console.log('Page: Domaine initialized');
}


// ==========================================
// ===== From src/scripts/page-index.ts =====
// ==========================================

function initPage() {
    console.log('Page: Home initialized');

    // Example: Add specific logic for the Hero section or specific animations here
}


// ==========================================
// ===== From src/scripts/page-oenotourisme.ts =====
// ==========================================

function initPage() {
    console.log('Page: Oenotourisme initialized');
}


// ==========================================
// ===== From src/scripts/page-vins.ts =====
// ==========================================

function initPage() {
    console.log('Page: Vins initialized');
}

